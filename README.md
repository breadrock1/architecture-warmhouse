# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование

«Тёплый дом» — это небольшая компания, которая организует удалённое управление отоплением в доме. Недавно она 
выиграла тендер и получила заказ на создание экосистемы умных посёлков на территории нескольких регионов страны.

Для выполнения заказа компании нужно расширить функционал своей системы и масштабировать её. Пользователи должны иметь 
возможность управлять не только отоплением, но и умными устройствами в доме. Чтобы подключиться к экосистеме, в домах 
должны быть установлены специальные датчики и реле. Сейчас они есть только в половине домов, которые хотят подключиться.

Состояние компании в настоящий момент не позволяет в полной мере реализовать новые бизнес-цели. Для этого требуется 
пересмотр и оптимизация всей экосистемы.

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- Система предоставляет возможность управлять отоплением (вкл/откл);
- Система позволяет подключать новые датчики и устройства (только с помощью инженера);

**Мониторинг температуры:**

- Система собирает значения с датчиков температуры, подключенных к реле;
- Система предоставляет возможность пользователю получать актуальное значение температуры в помещении;

### 2. Анализ архитектуры монолитного приложения

Перечислите здесь основные особенности текущего приложения: 
какой язык программирования используется, 
какая база данных, 
как организовано взаимодействие между компонентами и так далее.

 - Приложения реализовано на языке Go;
 - Подключение к СУБД Postgres нативное через библиотеку `pgxpool`;
 - Архитектура приложения представляет из себя монолит (все компоненты системы в рамках одной кодовой базы);
 - Всё синхронно. Никаких асинхронных вызовов, микросервисов и реактивного взаимодействия в системе нет;
 - Всё управление идёт от сервера к датчику. Данные о температуре также получаются через запрос от сервера к датчику;
 - Структура проекта разделена по типу функциональности, т.е. handler, models, routers отделены. DDD не используется;
 - Масштабируемость ограничена - нет возможности масштабировать отдельные части проекта;
 - Deploy с помощью Docker - легче настроить и развернуть проект, но требует остановки при приложения. 

### 3. Определение доменов и границы контекстов

Опишите здесь домены, которые вы выделили.

 - **Домен: Пользователи**
   - поддомен личный кабинет пользователя
     - контекст: регистрация нового пользователя
     - контекст: установка и подключение устройств
 - **Домен: Устройства**
   - поддомен: подключение устройств
     - контекст: установка и настройка устройства (с/без выезда специалиста)
   - поддомен: управление устройствами
     - контекст: включение/отключение устройств
     - контекст: изменение настроек работы устройств
   - поддомен: мониторинг устройств
     - контекст: сбор телеметрии устройств
     - контекст: предоставление информации о телеметрии


### **4. Проблемы монолитного решения**

- Cильная связанность компонентов, из-за чего в самом коде сложно будет, к примеру, сменить клиента для работы с БД; 
- Данную реализацию легко можно поддерживать и развивать, однако с добавлением новой функциональности будут возникать проблемы с поддержкой и развития кодовой базы;
- Текущее решение можно масштабировать, к примеру, если реплицировать монолит и шардировать PostgresDB, однако при росте нагрузки такое решение не сможет сохранять достурность и стабильность работы; 
- Сохраняется проблема с развертыванием, как минимум потому что необходима помощь специалиста для установки реле и устройств, а также требуется перезапуск контейнера.

Если вы считаете, что текущее решение не вызывает проблем, аргументируйте свою позицию.

### 5. Визуализация контекста системы — диаграмма С4

Здесь можно найти диаграмму контекста в модели C4:

```markdown
[c4-as-is-context-diagram](docs/diagrams/c4-as-is-context-diagram.puml)
```

# Задание 2. Проектирование микросервисной архитектуры

**Диаграмма контейнеров (Containers)**

[c4-to-be-containers-diagram](docs/diagrams/c4-to-be-containers-diagram.puml)

**Диаграмма компонентов (Components)**

[c4-to-be-components-users-diagram](docs/diagrams/c4-to-be-components-users-diagram.puml)
[c4-to-be-components-devices-diagram](docs/diagrams/c4-to-be-components-devices-diagram.puml)
[c4-to-be-components-monitoring-diagram](docs/diagrams/c4-to-be-components-monitoring-diagram.puml)

**Диаграмма кода (Code)**

[c4-to-be-code-devices-diagram](docs/diagrams/c4-to-be-code-devices-diagram.puml)

# Задание 3. Разработка ER-диаграммы

ER-диаграмму, отражающая ключевые сущности системы, их атрибуты и тип связей между ними:

[er-to-be-diagram](docs/diagrams/er-to-be-diagram.puml)

# Задание 4. Создание и документирование API

### 1. Тип API

В качестве типа API я выбрал часто используемое решение - REST API по следующей причине: быстрый переход между текущим 
и предлагаемым будущем решениями, это гораздо "дешевле", нежели использование более современных подходов, к примеру gRPC.
REST API позволяет легко интегрировать различные микросервисы, обеспечивая простоту, гибкость и скорость в разработке. 
В будущем, когда количество регионов будет увеличиваться, есть смысл задуматься о переходе с REST API, но сейчас - нет смысла.

### 2. Документация API

[openapi-swagger](docs/swagger.yml)

# Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

1) сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

```
	// If no location is provided, use a default based on sensor ID
	if location == "" {
		switch sensorID {
		case "1":
			location = "Living Room"
		case "2":
			location = "Bedroom"
		case "3":
			location = "Kitchen"
		default:
			location = "Unknown"
		}
	}

	// If no sensor ID is provided, generate one based on location
	if sensorID == "" {
		switch location {
		case "Living Room":
			sensorID = "1"
		case "Bedroom":
			sensorID = "2"
		case "Kitchen":
			sensorID = "3"
		default:
			sensorID = "0"
		}
	}
```

2) Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

3) Кроме того для smart_home приложения требуется база данных - добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

- Create Sensor
- Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.


